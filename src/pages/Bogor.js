import React, { Component } from 'react'
import { ScrollView, View } from 'react-native';

export default class Bogor extends Component{
    constructor(props){
        super(props);
        this.state={

        };
    }

    render(){
        return(
            <View>
                <ScrollView>
                    
                </ScrollView>
            </View>
        )
    }
}